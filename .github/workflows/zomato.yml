name: zomato
env:
  DOCKER_USER: ${{secrets.DOCKER_USERNAME}}
  DOCKER_PASSWORD: ${{secrets.DOCKERHUB_PASSWORD}}
  REPO_NAME: ${{secrets.REPO_NAME}}
on: 
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout code 
      uses: actions/checkout@v4
      
    
    - uses: actions/setup-node@v4
      with:
        node-version: 16
    - run: npm install  
    - run: npm ci
    
    # - name: SonarQube Scan
    #   uses: kitabisa/sonarqube-action@master
    #   with:
    #     host: ${{ secrets.SONAR_HOST_URL }}
    #     login: ${{ secrets.SONAR_TOKEN }}
    # - name: Depcheck
    #   uses: dependency-check/Dependency-Check_Action@main
    #   id: Depcheck
    #   with:
    #       project: 'zomato-clone'
    #       path: '.'
    #       format: 'HTML'
    # - name: Upload Test results
    #   uses: actions/upload-artifact@master
    #   with:
    #        name: Depcheck report
    #        path: ${{github.workspace}}/reports     
     
    # - name: Set up QEMU
    #   uses: docker/setup-qemu-action@v3
      
    # - name: Set up Docker Buildx
    #   uses: docker/setup-buildx-action@v4

    # - name: Run Trivy FS Scan
    #   run: |
    #     docker pull aquasec/trivy
    #     docker run -v $(pwd):/workspace aquasec/trivy fs . > trivyfs.txt
    - name: docker login
      run: | # log into docker hub account
          docker login -u $DOCKER_USERNAME -p $DOCKERHUB_PASSWORD  
   
    - name: Build the Docker image # push The image to the docker hub
      run: docker build . --file Dockerfile --tag $DOCKER_USERNAME/$REPO_NAME:1
    - name: Docker Push
      run: docker push $DOCKER_USERNAME/$REPO_NAME:1

    
